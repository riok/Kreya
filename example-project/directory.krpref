{
  "settings": [
    {
      "options": {
        "grpc": {
          "endpoint": "{{ env.grpc_endpoint }}"
        },
        "rest": {
          "endpoint": "{{ env.rest_endpoint }}"
        }
      }
    }
  ]
}